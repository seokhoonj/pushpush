name: check

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    # The full check lives in reusable-check.yml so the release gate in
    # publish.yml runs the identical job (PYTHON constitution Ch1 §1.8).
    uses: ./.github/workflows/reusable-check.yml
